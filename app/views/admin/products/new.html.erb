<div class="container">
  <div class="row">
   <div class="col">
    <%= render 'layouts/error_messages', validation: @product %>
    <h2>商品新規登録</h2>
     <%= form_with(model: [:admin, @product], local:true) do |f| %>

     <div class="field">
      <%= f.label :product_image, "商品画像" %><br />
      <%= f.attachment_field :product_image %>
     </div>

     <div class="field">
      <%= f.label :product_name, "商品名" %><br />
      <%= f.text_field :product_name %>
     </div>

     <div class="field">
      <%= f.label :product_description, "商品説明" %><br />
      <%= f.text_area :product_description %>
     </div>

     <div class="form_input">
      <%= f.label :genre_id, "ジャンル" %><br />
      <%= f.collection_select :genre_id, Genre.all, :id, :name, :include_blank => "ジャンルを選択してください" %>
     </div>

     <div class="field">
      <%= f.label :sales_price, "価格（税抜き）" %><br />
      <%= f.text_field :sales_price %> 円
     </div>

     <div class="field">
      <%= f.label :sales_status, "販売ステータス" %><br />
      <%= f.radio_button :sales_status, "true" %>販売中
      <%= f.radio_button :sales_status, "false" %>販売停止
     </div>

     <p></p>
     <div><%= f.submit '新規登録',class: "btn btn-success text-light" %></div>

     <% end %>
   </div>
 </div>
</div>